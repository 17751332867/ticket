<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE  mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.lut.ticket.dao.OrderMapper">
<resultMap id="BaseResultMap" type="com.lut.ticket.entity.Order">
            <result column="id" property="id" />
        <result column="passenger" property="passenger" />
        <result column="flightId" property="flightId" />
        <result column="ticketId" property="ticketId" />
        <result column="state" property="state" />
</resultMap>  
    
    <!-- ??? -->
    <sql id="baseColumns">
         t.id
        , t.passenger
        , t.flightId
        , t.ticketId
        , t.state
        </sql> 
    
    <!-- ???? -->
    <select id="listAll" resultMap="BaseResultMap">
SELECT         
        <include refid="baseColumns" />
FROM order t
</select>
 
 <!-- ?????????? -->
    <select id="getById" resultMap="BaseResultMap" parameterType="Integer">
SELECT         
        <include refid="baseColumns" />
FROM order t
WHERE id = #{id}
</select>

<!-- ?????? -->
    <insert id="insert" parameterType="com.lut.ticket.entity.Order"
keyProperty="id" keyColumn="id" useGeneratedKeys="true"
    >
INSERT INTO order
    <trim prefix="(" suffix=")" suffixOverrides=","> 
                    passenger,
                flightId,
                ticketId,
                state,
            </trim>
    <trim prefix="VALUES (" suffix=")" suffixOverrides=",">            
                        #{passenger},                   
                    #{flightId},                   
                    #{ticketId},                   
                    #{state},                   
            </trim>
</insert>
    
    <!-- ????NULL??? -->
    <insert id="insertIgnoreNull" parameterType="com.lut.ticket.entity.Order"
        keyProperty="id" keyColumn="id" useGeneratedKeys="true"
        >
        INSERT INTO order    
        <trim prefix="(" suffix=")" suffixOverrides=","> 

                                                                            <if test="passenger != null">
                   passenger,
                    </if>
                                                                <if test="flightId != null">
                   flightId,
                    </if>
                                                                <if test="ticketId != null">
                   ticketId,
                    </if>
                                                                <if test="state != null">
                   state,
                    </if>
                                    </trim>
        <trim prefix="VALUES (" suffix=")" suffixOverrides=",">            
                                                                            <if test="passenger != null" >
                    #{passenger},                    
                    </if>
                                                                <if test="flightId != null" >
                    #{flightId},                    
                    </if>
                                                                <if test="ticketId != null" >
                    #{ticketId},                    
                    </if>
                                                                <if test="state != null" >
                    #{state},                    
                    </if>
                                    </trim>
    </insert>

<!-- ??,?????? -->
    <update id="update" parameterType="com.lut.ticket.entity.Order">
    UPDATE order
     <set>
                                     passenger=#{passenger},        
                                flightId=#{flightId},        
                                ticketId=#{ticketId},        
                                state=#{state},        
                   </set>
    WHERE id = #{id}
    </update>  
    
    
    <!-- ????NULL??? -->
    <update id="updateIgnoreNull" parameterType="com.lut.ticket.entity.Order">
    UPDATE order
    <set>
                                                    <if test="passenger != null" >
                passenger=#{passenger},                 
                </if>
                                                <if test="flightId != null" >
                flightId=#{flightId},                 
                </if>
                                                <if test="ticketId != null" >
                ticketId=#{ticketId},                 
                </if>
                                                <if test="state != null" >
                state=#{state},                 
                </if>
                        </set>
    WHERE id = #{id}
    </update>


<!-- ???????? -->
<delete id="delete" parameterType="com.lut.ticket.entity.Order">
UPDATE order
SET is_deleted=1
WHERE id = #{id}
</delete>


</mapper>